import React from 'react';
import styles from './construct.module.css';

const construct = (props) => {
	//console.log("Props from Home", props.id)
	return <div className={styles.constructPage} />;
};

export default construct;
